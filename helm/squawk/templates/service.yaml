apiVersion: v1
kind: Service
metadata:
  name: {{ include "squawk.fullname" . }}
  labels:
    {{- include "squawk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "squawk.selectorLabels" . | nindent 4 }}
